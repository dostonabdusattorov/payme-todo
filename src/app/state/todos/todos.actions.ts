import { createAction, props } from '@ngrx/store';

export const getTodos = createAction('[Todos] Load todos');
export const getTodosSuccess = createAction('[Todos] Load todos');
export const getTodosFailure = createAction('[Todos] Load todos');
